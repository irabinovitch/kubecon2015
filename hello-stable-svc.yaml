apiVersion: v1
kind: Service
metadata:
  name: hello-stable
  labels:
    app: hello
    track: stable
spec:
  type: NodePort
  selector:
    app: hello
    track: stable
  ports:
  - name: http
    nodePort: 36001
    port: 80
    protocol: TCP
